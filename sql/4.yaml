operations:
  - alter_column:
      table: runs
      column: monitor_id
      up: monitor_id
      down: monitor_id
      references:
        column: id
        table: monitors
        name: fk_monitor_id
        on_delete: CASCADE
