operations:
  - add_column:
      table: monitors
      up: completed_at is not null
      column:
        name: paused
        type: boolean
        default: "false"
  - drop_column:
      table: monitors
      column: completed_at
